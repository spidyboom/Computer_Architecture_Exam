# **138. Какие подходы существуют к решению проблемы изоляции данных в памяти (статика, куча, стек) при многозадачности?**

- статика (статическая память) - секция .data
- куча (динамическая память) - можно выделить там себе место, можно освободить. Находится под управлением выполняемой задачи
- стек (автоматическая память) - выделяется и освобождается автоматически в зависимости от того, насколько глубоко мы ушли во вложенные процедуры

Многозадачность требует в том числе совмещение задач по используемым ресурсам, в частности по памяти. Но помимо задачи совмещения также стоит задача изоляции, чтобы одна задача не имела доступа к памяти другой задачи, иначе мы получаем уязвимости безопасноти и ситуации, когда одна задача по глупости программиста крашит другую / крашит систему.

Есть три основных подхода к решению проблемы изоляции памяти, все они работают в runtime:

- Банки памяти - Сегодня уже мертвая технология. Классически это вообще не средство изоляции. Идея: давайте подключим к процессору несколько микросхем памяти. Например повесим их на одну шину и будем переключаться между ними, выбирая с кем именно мы сейчас работаем. Это можно использовать для аппаратной изоляции задач: задача физически не сможет прочитать что-то из другой микросхемы, ведь процессор позволяет ей читать только из своей.

- Сегментная память см билеты дальше

- Виртуальная память см билеты дальше
